请阅读这个代码项目，并完成以下任务：

1. 分析这个项目的输入数据结构。
2. 说明输入数据的整体组织方式，例如：
   - 输入来自哪些文件、函数、接口或配置；
   - 数据是 JSON、字典、类对象、表格，还是其他结构；
   - 各字段的名称、类型、是否必填、默认值及含义；
   - 如果存在嵌套结构，请逐层解释；
   - 如果项目中有多种输入格式，请分别说明它们之间的关系和使用场景。
3. 基于代码实现推断数据流：
   - 输入数据是在哪里被读取的；
   - 如何被解析、校验、转换和传递；
   - 最终被哪些核心模块或函数消费。
4. 输出一份结构化文档，文档应包含：
   - 项目输入数据结构总览
   - 关键数据类型/字段说明
   - 示例输入
   - 数据流说明
   - 需要特别注意的约束、边界条件或隐含假设
5. 如果代码中信息不完整，请明确标注“基于代码推断”与“不确定之处”，不要臆造。

请以 Markdown 格式输出文档，要求内容清晰、分层明确，适合开发者阅读。