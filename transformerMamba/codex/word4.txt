你现在的第一阶段任务不是修改代码，而是先基于当前代码库，验证已有算法分析报告中的结论是否成立，并输出一份“最小改动实施方案”。

注意：这一阶段不要修改代码，不要提交补丁，不要重构。你的任务是“核实 + 规划”。

请重点验证以下问题是否在真实代码中成立：

1. `dataset.py` 中 `normalize_url` 是否默认统一小写，以及这是否会影响 URL 大小写模式信息
2. `models/detector.py` 是否内部固定使用 0.5 阈值，而 `predict.py` / `utils.py` 是否又使用外部 threshold，导致决策逻辑不统一
3. `predict.py` 中 `predict_records` 是否一次性处理全部 records，缺少分批推理
4. 数据输入是否过度依赖 `dict.get` 默认值，缺少明确 schema 校验
5. `models/fusion.py` 是否缺少可观测的 gate / 融合诊断信息
6. `main_logits` 命名是否存在语义歧义
7. 哪些问题是确定的 bug，哪些只是设计选择，哪些暂时无法确认

输出要求：

# 输出文件
VALIDATION_AND_MINIMAL_CHANGE_PLAN.md

# 文档结构
## 1. 报告结论核实结果
按“结论 / 是否成立 / 代码证据 / 备注”逐项列出

## 2. 必须立即处理的问题（P0）
只列真正影响正确性、一致性、训练推理接口的问题

## 3. 建议随后处理的问题（P1）
列维护性、可观测性、性能方面的问题

## 4. 暂时不要动的问题
列出不建议现在改的部分，并说明原因

## 5. 最小改动实施方案
要求：
- 改动范围尽量小
- 不推翻现有主链路
- 尽量保持 checkpoint 兼容
- 明确哪些文件要改
- 明确哪些接口会受影响
- 明确回归测试要检查什么

## 6. 风险说明
说明如果直接改会有什么风险：
- 数据分布变化
- checkpoint 不兼容
- 指标不可比
- 决策语义漂移
- 训练 / 推理行为不一致

重要要求：
1. 必须以实际代码为准，不要只复述报告
2. 不确定的地方必须标明“待确认”
3. 这一阶段不要修改代码
4. 输出语言用中文