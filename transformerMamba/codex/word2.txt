请修改文档中关于 URL 表征算法预处理的描述，要求如下：

1. 将“对 URL 做标准化处理：去空白、转小写”中的“转小写”删除。
2. 明确改为：预处理时仅去除无意义空白字符，保留 URL 原始大小写，不进行统一小写化。
3. 在该处补充原因说明：
   - URL 中的大小写本身可能携带恶意模式信息，尤其是在路径、参数、token、伪随机串等部分；
   - 域名部分通常大小写不敏感，但 URL 的其余部分不适合直接统一成小写；
   - 攻击者可能利用大小写混合制造视觉混淆、模式扰动和 token 差异，因此大小写模式本身属于可被模型利用的判别信号。