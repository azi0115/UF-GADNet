你现在的任务不是修改代码，而是系统性梳理当前项目中“所有算法相关代码”的设计逻辑，并输出一份可以交给另一个 AI/架构分析者继续深度分析的完整文档。

请严格按照以下要求执行：

# 任务目标
阅读当前项目代码库中所有与算法有关的代码、配置、调用链和数据流，产出一份高质量的 Markdown 文档，完整说明：
1. 项目里有哪些算法模块
2. 每个算法模块解决什么问题
3. 它们之间如何协作
4. 核心设计逻辑是什么
5. 关键实现细节在哪里
6. 目前设计中的假设、边界、风险、耦合点、可优化点分别是什么

**注意：你的目标不是泛泛总结，而是要让一个没有看过此项目代码的人，看完文档后能快速理解整个算法系统的设计与实现逻辑。**

# 扫描范围
请优先覆盖以下内容：
- 所有算法核心代码
- 模型定义、推理逻辑、训练逻辑、特征工程、后处理逻辑
- 策略、规则、打分、排序、召回、过滤、优化、搜索、路径规划、推荐、分类、聚类、预测、估计等算法相关模块
- 与算法行为强相关的配置文件
- 算法调用入口、服务层调用链、数据处理流水线
- 关键工具函数、公共基类、抽象接口
- 与算法效果强相关但容易被忽略的“参数控制逻辑”

如项目很大，请不要只看文件名判断，要根据真实调用链和代码内容识别哪些模块属于算法逻辑。

# 输出文件
请输出一个 Markdown 文档，文件名为：

ALGORITHM_DESIGN_LOGIC_REPORT.md

# 文档结构要求
文档必须包含以下章节，并尽量写充实：

## 1. 项目算法系统总览
- 这个项目的核心业务目标是什么
- 算法系统在整个项目中的职责是什么
- 算法系统整体架构图（用 Mermaid 或文本结构图表示）
- 从输入到输出的整体数据流 / 决策流

## 2. 算法模块清单
请列出所有识别到的算法模块，并用表格说明：
- 模块名称
- 文件路径
- 入口函数 / 核心类
- 主要职责
- 上游依赖
- 下游影响
- 重要程度（高/中/低）

## 3. 每个算法模块的详细设计逻辑
对每个重要模块单独展开说明，至少包括：
- 模块目标
- 设计思路
- 输入 / 输出
- 核心类、函数、方法
- 关键参数及其含义
- 主要处理流程（建议给出伪代码或流程图）
- 与其他模块的关系
- 为什么要这样设计（如果能从代码中推断）
- 是否存在隐藏约束、默认假设、硬编码规则、魔法数字
- 该模块中最关键的 3~10 段代码位置（给出文件路径 + 函数名/类名）

## 4. 核心算法流程拆解
如果项目包含完整主流程，请按执行顺序拆解，例如：
- 数据读取
- 数据清洗/预处理
- 特征构造
- 模型/策略执行
- 打分/决策/排序
- 后处理
- 输出生成

对每一步说明：
- 做了什么
- 为什么需要这一步
- 输入输出如何变化
- 哪些参数会显著影响结果
- 哪些步骤最脆弱

## 5. 配置与参数系统
- 项目中有哪些配置影响算法行为
- 配置从哪里加载
- 配置如何覆盖默认值
- 哪些参数最重要
- 哪些参数之间可能存在耦合或冲突
- 是否存在“改一个参数会连带影响多个模块”的情况

## 6. 调用链与依赖关系
请梳理：
- 算法入口在哪里
- 关键调用链路径
- 模块依赖图
- 是否有循环依赖、隐式依赖、跨层调用、耦合过深的问题

## 7. 设计假设、边界与风险
请明确指出：
- 代码隐含了哪些业务假设
- 依赖了哪些数据前提
- 哪些边界条件处理不充分
- 哪些逻辑容易出错
- 哪些地方可读性差或维护成本高
- 哪些设计可能影响性能、稳定性、可扩展性

## 8. 技术债与优化建议
请结合代码实际情况给出：
- 冗余逻辑
- 重复实现
- 命名不清
- 职责混乱
- 参数散落
- 缺少抽象
- 可测试性差
- 可观测性不足
- 性能瓶颈候选点

每条建议都尽量对应到具体文件或代码位置。

## 9. 仍不确定的点
如果你无法从当前代码中 100% 确认某些设计意图，请不要编造。
请单独列出：
- 不确定点
- 你为什么不确定
- 你依据了哪些代码线索
- 还需要查看哪些信息才能确认

## 10. 给下一位分析者的摘要
最后请输出一个“可供下一个 AI 继续分析”的摘要，包括：
- 这个项目最核心的算法主线
- 最值得重点审查的模块
- 最可能存在设计风险的 5 个点
- 最值得优化的 5 个点
- 建议下一步深入分析的顺序

# 写作要求
请严格遵守：
1. 不要只做表面总结，必须结合真实代码结构和调用关系。
2. 不要只按文件罗列，要按“算法逻辑”组织内容。
3. 不确定时明确标记“推测 / 待确认”，不要脑补。
4. 尽量引用具体文件路径、类名、函数名、配置名。
5. 尽量给出伪代码、流程图、结构图，而不是只写自然语言。
6. 不要输出空泛评价，所有结论尽量对应代码证据。
7. 如果代码库很大，优先覆盖最核心、最常被调用、最影响结果的算法模块，并在文档里说明哪些区域已覆盖、哪些未深入。
8. 文档内容要面向“架构分析”和“后续优化评审”，不是面向业务汇报。
9. 输出语言使用中文。
10. 最终只输出 Markdown 文档正文内容。

开始前，请先：
- 识别算法相关目录和入口
- 梳理调用链
- 再生成最终文档

补充要求：
- 对每个重要算法模块，增加“证据索引”小节，列出支撑结论的文件路径、函数名、类名。
- 对每个优化建议，标注优先级：P0 / P1 / P2。
- 对每个风险点，标注风险类型：正确性 / 性能 / 稳定性 / 可维护性 / 可扩展性。
- 若存在训练与推理逻辑不一致、线上线下逻辑不一致、配置漂移、规则与模型冲突等问题，请单独重点标出。
- 若存在多个算法版本并存，请整理版本差异、废弃逻辑和实际生效路径。